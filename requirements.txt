# AcuSound Backend Dependencies
# NOTE: torch and torchvision are installed separately via the Render Build Command
# using the CPU-only index to avoid the full CUDA bundle (~4x smaller).
# Build command:
#   pip install -r requirements.txt && pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# Web framework
fastapi
uvicorn[standard]

# HTTP client (for OpenRouter calls)
httpx

# File upload support for FastAPI
python-multipart

# Model architecture
timm

# Audio processing
librosa
soundfile

# Image processing
Pillow

# Visualization
matplotlib

# GradCAM for explainability
grad-cam

# Numerical computing
numpy
