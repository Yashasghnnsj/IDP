export function computeMFCC(
  audioBuffer: Float32Array,
  sampleRate: number
): Float32Array {

  // Placeholder MFCC feature vector
  // Replace later with real DSP processing

  return new Float32Array(13);
}
